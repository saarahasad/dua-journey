# Dua audio recitations

Place MP3 files here named by dua ID to enable audio playback on Step 1 and Step 6.

Example: `dua-praise-1.mp3` for the dua with id `dua-praise-1`.

Alternatively, set `audioUrl` in `src/data/duas.json` for each dua to use a custom URL.
